{
  "name": "slack-notebooklm-sync",
  "version": "1.0.0",
  "description": "Syncs Slack channel messages to Google Docs for NotebookLM",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "deploy": "gcloud functions deploy slack-sync --gen2 --runtime nodejs20 --trigger-http --allow-unauthenticated --entry-point slackWebhook --source ."
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.3.0",
    "@slack/web-api": "^7.0.0",
    "googleapis": "^130.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
